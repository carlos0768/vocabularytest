  実装指示書（タスク #2, #3, #5, #6）

  タスク #2: 単語帳カードの進捗表示を最適化

  ファイル: src/components/project/project-card.tsx

  変更内容: <CardContent> 内のプログレス表示（70〜81行目）を条件分岐する。

  ルール:
  - progress === 0 の場合 → プログレスバーと「0語 習得済み」を非表示にし、代わりに 未学習 バッジ（グレー背景、小さめ）を表示
  - progress > 0 && progress < 100 の場合 → 現状通りプログレスバー + 習得数を表示
  - progress === 100 の場合 → プログレスバーの代わりに 習得完了! バッジ（success色）を表示

  イメージ:
  // 70行目〜の <CardContent> を置き換え
  <CardContent className="pt-0">
    {progress === 0 ? (
      <span className="inline-flex items-center gap-1 text-xs text-[var(--color-muted)] bg-[var(--color-border-light)] px-2 py-0.5 rounded-full">
        未学習
      </span>
    ) : progress >= 100 ? (
      <span className="inline-flex items-center gap-1 text-xs text-[var(--color-success)] bg-[var(--color-success-light)] px-2 py-0.5 rounded-full">
        <Icon name="check_circle" size={14} />
        習得完了
      </span>
    ) : (
      <div>
        <div className="flex items-center justify-between text-xs text-[var(--color-muted)] mb-2">
          <span>習得率</span>
          <span>{Math.round(progress)}%</span>
        </div>
        <div className="progress-bar">
          <div className="progress-bar-fill" style={{ width: `${progress}%` }} />
        </div>
        <p className="mt-2 text-xs text-[var(--color-muted)]">{masteredCount}語 習得済み</p>
      </div>
    )}
  </CardContent>

  ---
  タスク #3: 長いタイトルの省略処理

  ファイル: src/components/project/project-card.tsx

  変更内容: 52行目の <CardTitle> を修正。

  現状:
  <CardTitle className="line-clamp-2">{project.title}</CardTitle>

  変更後:
  <CardTitle className="line-clamp-1 break-all">{project.title}</CardTitle>

  - line-clamp-2 → line-clamp-1 に変更（1行で省略）
  - break-all を追加（連続英字でもカード幅を突き破らない）

  ---
  タスク #5: サイドバーから「スキャン」を削除

  ファイル: src/components/ui/Sidebar.tsx

  変更内容: navItems 配列（15〜58行目）から「スキャン」エントリを削除。

  削除対象（33〜38行目）:
  {
    href: '/scan',
    icon: 'center_focus_weak',
    label: 'スキャン',
    matchPaths: ['/scan'],
  },

  この行を丸ごと削除する。スキャンへのアクセスは以下で担保済み:
  - モバイル: BottomNavの中央CTAボタン（変更不要）
  - デスクトップ: ダッシュボードのクイックアクション（変更不要）
  - 後日タスク #1,#4 で更に整理予定

  ---
  タスク #6: セクション間余白の統一

  ファイル: src/app/page.tsx

  変更内容: メインコンテンツ領域の余白を8px倍数グリッドに統一。

  1276行目 の <main> タグ:
  // 現状
  <main className="flex-1 max-w-lg lg:max-w-5xl mx-auto px-4 lg:px-8 py-6 w-full space-y-6">

  // 変更後
  <main className="flex-1 max-w-lg lg:max-w-5xl mx-auto px-4 lg:px-8 py-8 w-full space-y-8">

  - py-6（24px） → py-8（32px）
  - space-y-6（24px） → space-y-8（32px）

  セクション内のサブ余白（space-y-3等）はそのまま。セクション間の「呼吸」を広げる目的。

  ---
  まとめ
  ┌─────────────────┬──────────────────┬──────────┬──────────────┐
  │     タスク      │     ファイル     │ 変更規模 │ 推定所要時間 │
  ├─────────────────┼──────────────────┼──────────┼──────────────┤
  │ #2 進捗表示     │ project-card.tsx │ 〜15行   │ 15分         │
  ├─────────────────┼──────────────────┼──────────┼──────────────┤
  │ #3 タイトル省略 │ project-card.tsx │ 1行      │ 2分          │
  ├─────────────────┼──────────────────┼──────────┼──────────────┤
  │ #5 スキャン削除 │ Sidebar.tsx      │ 6行削除  │ 2分          │
  ├─────────────────┼──────────────────┼──────────┼──────────────┤
  │ #6 余白統一     │ page.tsx         │ 1行      │ 2分          │
  └─────────────────┴──────────────────┴──────────┴──────────────┘