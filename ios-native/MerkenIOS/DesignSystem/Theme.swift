import SwiftUI

enum MerkenTheme {
    static let bgTop = Color(red: 0.03, green: 0.08, blue: 0.16)
    static let bgBottom = Color(red: 0.01, green: 0.03, blue: 0.09)

    static let accentBlue = Color(red: 0.08, green: 0.50, blue: 0.93)
    static let accentBlueStrong = Color(red: 0.05, green: 0.42, blue: 0.83)

    static let success = Color(red: 0.12, green: 0.80, blue: 0.48)
    static let warning = Color(red: 0.96, green: 0.67, blue: 0.15)
    static let danger = Color(red: 0.94, green: 0.35, blue: 0.35)

    static let primaryText = Color.white
    static let secondaryText = Color.white.opacity(0.72)
    static let mutedText = Color.white.opacity(0.58)
}
