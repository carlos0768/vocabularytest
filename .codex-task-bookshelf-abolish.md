# Task: 本棚ページ廃止 & 単語帳ページUI統合

Branch: `wip/bookshelf-abolish-ui-unify` (already checked out)

## Overview
Carlosの改良案に基づき、本棚(collections)ページを廃止し、単語帳(projects)ページにすべてを統合する。

## Step 0: バグ修正（最優先）
**問題**: 既存の単語帳に単語を追加しようとすると、既存単語帳に追加されず新規単語帳が生成されてしまう。

**調査ポイント**:
- `src/app/page.tsx` L984-991: `sessionStorage.setItem('scanvocab_existing_project_id', currentProject.id)` が正しくセットされているか
- `src/app/scan/confirm/page.tsx`: `getInitialData()` で `existingProjectId` を読み取り、`handleSaveProject` で既存プロジェクトに追加するロジック
- `isAddingToExisting` フラグの流れを追跡し、どこで途切れているか特定

**修正**: バグ原因を特定し修正する。

## Step 1: 本棚ページ廃止
- `src/app/collections/` ディレクトリは残すが、`/collections` へアクセスしたら `/projects` にリダイレクトする簡易ページに置き換える
- ナビゲーション (`src/components/ui/bottom-nav.tsx`, `src/components/ui/Sidebar.tsx`) には既に「本棚」リンクはないので確認のみ

## Step 2: 単語帳ページ上部UI再設計 (`src/app/projects/page.tsx`)
既存の単語帳ページの上部に以下を追加:

### 2a. 単語帳一覧 + 追加ボタン
- ヘッダーの下に、横スクロール可能な単語帳カード一覧を配置（小さめのカード）
- 右端に「＋追加」ボタン（スキャンページへ遷移）

### 2b. 学習モードセクション
単語帳一覧の下に、3つの学習モードボタンを横並びで配置:
1. **フラッシュカード** → `/flashcard` へ遷移
2. **自己評価** → `/quiz` へ遷移  
3. **マッチ** → 未実装なら disabled 表示

デザイン: カード型ボタン、アイコン + ラベル

## Step 3: ソート「新しく入れた順」追加
- `src/app/projects/page.tsx` の `sortBy` stateに `'added'` を追加
- 単語帳のソートで、`createdAt` ではなく最後に単語が追加された日時でソートする
- ただし、現在のProjectデータに「最後に単語追加された日時」がない場合は `createdAt` でのソートを流用し、コメントで「TODO: lastWordAddedAt tracking」と記載

## Step 4: 追加タイミング別の太線区切り
- 単語帳内の単語一覧で、追加タイミング（スキャンバッチ）ごとに太線で区切る
- `src/app/project/[id]/page.tsx` を確認し、単語の `createdAt` をグルーピングして太線区切りを追加
- グルーピングは同じ分(minute)に作成された単語をひとまとめにする

## 重要な制約
- **新しいnpmパッケージの追加禁止** — 既存の依存関係のみで実装
- 既存のデザインシステム (`var(--color-*)`, `Icon`, `Button`, etc.) を使用
- TypeScript strict mode対応
- 変更後、`npm run build` でビルドが通ることを確認

## コミット
各ステップ完了ごとにコミットする:
- `fix: resolve bug where adding to existing project creates new one`
- `refactor: redirect /collections to /projects`
- `feat: add learning mode section and horizontal project cards to projects page`
- `feat: add word grouping by scan batch with dividers`

最後に `git push origin wip/bookshelf-abolish-ui-unify` でプッシュ。

When completely finished, run this command to notify me:
openclaw system event --text "Done: Bookshelf abolish & projects page UI unification complete" --mode now
